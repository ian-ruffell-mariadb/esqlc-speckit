#pragma SQL
long sqlcode;
EXEC SQL BEGIN DECLARE SECTION;
  short min_num;
EXEC SQL END DECLARE SECTION;
EXEC SQL DECLARE ficur CURSOR FOR SELECT part_num FROM parts WHERE part_num >= :min_num;
int main(void) {
  EXEC SQL OPEN ficur;
  EXEC SQL FETCH ficur INTO :never_declared;
  return 0;
}
