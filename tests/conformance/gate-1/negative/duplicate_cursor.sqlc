#pragma SQL
long sqlcode;
EXEC SQL BEGIN DECLARE SECTION;
  short min_num;
EXEC SQL END DECLARE SECTION;
EXEC SQL DECLARE dupcur CURSOR FOR SELECT part_num FROM parts WHERE part_num >= :min_num;
EXEC SQL DECLARE dupcur CURSOR FOR SELECT part_num FROM parts WHERE part_num >= :min_num;
int main(void) { return 0; }
