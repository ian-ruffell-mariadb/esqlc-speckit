#pragma SQL
long sqlcode;
EXEC SQL BEGIN DECLARE SECTION;
  short part_num;
EXEC SQL END DECLARE SECTION;
int main(void) {
  EXEC SQL SELECT part_desc INTO :never_declared FROM parts WHERE part_num = :part_num;
  return 0;
}
